# minimal-project — Agent Team Context

A bare project used by S9 unit and manual smoke tests. Lead context
injection should pick up this file via `build_lead_context()`.

## Stack
Just enough YAML and markdown to exercise the orchestrator.

## Rules
- Reply via `agent-team mail send` — never assume you can call out.
- Use `agent-team task` for the shared task board.
