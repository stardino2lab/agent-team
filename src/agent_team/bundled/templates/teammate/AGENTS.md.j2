# Teammate: {{ teammate_name }} ({{ persona_name }})

Session: `{{ session_id }}`
Project: `{{ project_path }}`

## Your role

{{ spawn_prompt }}

## When you are ready

Run this once, after reading this brief and before starting work, to signal the
orchestrator that you are ready:

```powershell
agent-team teammate ready --session {{ session_id }} --as {{ teammate_name }}
```

## Coordination (required)

Use these CLI commands from the project directory:

```powershell
agent-team mail send --session {{ session_id }} --as {{ teammate_name }} --to lead --body "your message"
agent-team mail read --session {{ session_id }} --as {{ teammate_name }} --since last
agent-team task list --session {{ session_id }}
agent-team task claim --session {{ session_id }} --id <task-id> --assignee {{ teammate_name }}
agent-team task complete --session {{ session_id }} --id <task-id>
```

## Rules

- Read TEAM.md in project root
- Mail the lead when blocked or done
- Do not spawn other teammates
