# 작업 규칙

## 계획 문서 우선 (Plan-first)

코드를 작성하거나 수정하기 전에 **항상 계획 문서를 먼저 작성**한다. 순서는 다음과 같다:

1. **계획 문서 작성** — 작업 시작 전, `plans/` 디렉토리에 마크다운 계획 문서를 만든다.
   - 파일명: `plans/YYYY-MM-DD-<짧은-슬러그>.md`
   - 포함할 내용: 목표 / 변경할 파일 목록 / 단계별 작업 / 검증 방법(테스트·린트) / 위험 요소
   - `writing-plans` 스킬이 있으면 그 형식을 따른다.

2. **검토 대기** — 계획을 사용자에게 제시하고 **명시적 승인을 받기 전까지 구현(파일 편집)을 시작하지 않는다.**

3. **실행** — 승인된 계획에 따라 구현한다. `executing-plans` 스킬이 있으면 그 흐름을 따른다.

4. **편차 발생 시** — 구현 중 계획에서 벗어나야 하면, 먼저 멈추고 변경 사항을 설명한 뒤 다시 승인을 받는다.

### 예외 (계획 문서 없이 진행해도 되는 경우)
- 오타·주석·문서 한 줄 수정 같은 사소한 변경
- 사용자가 명시적으로 "계획 없이 바로 해줘"라고 지시한 경우

이 두 경우가 아니면 **계획 없이 구현하지 않는다.**
